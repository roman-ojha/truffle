// SPDX-License-Identifier: MIT

pragma solidity >=0.6.0 <0.9.0;

/*
 *) Initialized In VScode
    -> go to project directory:
      -> truffle init (to initilaized truffle project)
   -> Folder directory:
      -> contracts (contract contain all the smart contract that we will create)
      -> migrations (when we will deploy contract at that time we will use migration)
      -> test (for testing code)
      -> truffle-config.js (all the configrution for truffle)
 */

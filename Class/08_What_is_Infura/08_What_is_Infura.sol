// SPDX-License-Identifier: MIT

pragma solidity >=0.6.0 <0.9.0;

/*
    *) What is Infura:
        -> Infura is a full node where with the help of this we can easily connect to ethereum blockchain
        -> It means that if we want to deploy contract on 'mainnet' or 'testnet' the we can do that by using Infura

    -> so firstly go to official website of infura and signin
    -> and create an new porject
    -> after create project we can go to endpoints and chose the endpoint like 'mainnet' or 'testnet'
    -> and it will give the link for us to deploy on 'mainnet' or 'testnet'
    -> in another class we will learn about to deploy contract on testnet
*/

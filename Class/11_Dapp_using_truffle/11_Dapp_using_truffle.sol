// SPDX-License-Identifier: MIT

pragma solidity >=0.6.0 <0.9.0;

/*
    -> to see affect that we have done on contract on client site we have to first migrate the contract again
    -> truffle migrate
    -> if now migrating then:
        -> truffle migrate --reset
    -> here we have done some change on 'client/src/App.js'
*/
